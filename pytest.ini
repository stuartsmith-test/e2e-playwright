# pytest.ini — project-wide pytest configuration

[pytest]
# Allow tests to import from the project root (e.g., `utils.*`)
pythonpath = .

# Shared origin for both UI (pytest-playwright) and API tests.
# The `base_url` value is read by the pytest-base-url plugin,
# and the `api_request_context` fixture in conftest.py uses the same value.
base_url = http://localhost:3000

